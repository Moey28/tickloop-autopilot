# Stub collector: simulate a successful Polymarket/Hashdive pull.
# Replace later with real API/subgraph calls.
print("[collector] pulling data… (stub)")
print("[collector] wrote sample data to ./data (stub)")
exit(0)  # success so the whole pipeline passes now
